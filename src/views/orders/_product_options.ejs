<option value="">Selecione o produto</option>
<% products.forEach((product) => { %>
  <option
    value="<%= product.id %>"
    <%= String(typeof selectedProductId === 'undefined' ? '' : selectedProductId) === String(product.id) ? 'selected' : '' %>
    data-unit-price="<%= Number(product.unitPrice ?? 0).toFixed(2) %>"
    data-box-price="<%= product.boxPrice === null || product.boxPrice === undefined ? '' : Number(product.boxPrice).toFixed(2) %>"
  >
    <%= product.name.toUpperCase() %> - <%= product.productCategory?.name || product.category %> - Un. R$ <%= Number(product.unitPrice ?? 0).toLocaleString('pt-BR', { minimumFractionDigits: 2 }) %>
    <% if (product.boxPrice) { %>
      | Cx. R$ <%= Number(product.boxPrice).toLocaleString('pt-BR', { minimumFractionDigits: 2 }) %>
    <% } %>
    | C&oacute;d. <%= product.code %>
  </option>
<% }) %>
<% if (!products.length) { %>
  <option value="">Nenhum produto encontrado</option>
<% } %>
